Set up git-update.rb and git-publish.rb to be run via the git alias system.

IE.

>> git config --global alias.update
!ruby ~/git/gx/git-update.rb

>> git config --global alias.publish
!ruby ~/git/gx/git-publish.rb

Then you can just use the commands anywheres! Enjoy!
