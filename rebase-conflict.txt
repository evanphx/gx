# git pull --rebase
* adds conflict markers
* edit file
* require add files
* git rebase --continue

# git pull
* adds conflict markers
* edit file
* add files
* git commit # contains filled in merge commit message


notes:

== git merge-base ==
Detects, for 2 hashs, where to merge from (super awesome)


current errors:

== git rebase ==
mid rebase, shows .dotest as untracked

== git rebase --continue ==
Gives an error in 'cat' if there is no outstanding rebase
